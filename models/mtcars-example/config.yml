# Mandatory Fields (auto-generated)
model_metadata:
  model_uid: 1L2IoumkZf4VonGRdh3C
  model_creation_date: 2019-02-22 01:27:32 #UTC Time Zone
  model_author: harel.l
  model_parent: 
  model_name: mtcars-example
# Mandatory Fields (manually-defined)
data_source:
  dataset_name: mtcars
  dataset_key_column: ROWID
# Optional Fields
parameters: 
# Specific packages, which are not declared on a project level
packages:
  CRAN:
    stats: '>= 3.3.0' # required for the linear model (just an example)
